<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deleted Workouts</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css">
</head>
<body>
    <h1>Deleted Workouts</h1>
    <ul>
        <% deletedWorkouts.forEach(workout => { %>
            <li>
                <strong>Workout Name:</strong> <%= workout.workout_name %><br>
                <strong>Type:</strong> <%= workout.workout_type %><br>
                <strong>Duration:</strong> <%= workout.duration_minutes %> minutes<br>
                <strong>Calories Burned:</strong> <%= workout.calories_burned %><br>
                <% if (workout.notes) { %>
                    <strong>Notes:</strong> <%= workout.notes %><br>
                <% } %>
                <form action="/workouts/restore/<%= workout.workout_id %>" method="POST">
                    <button type="submit">Restore</button>
                </form>
            </li>
        <% }); %>
    </ul>
    <a href="/workouts">Back to Workouts</a>
    <br>
    <a href="/">Home</a>
</body>
</html>

